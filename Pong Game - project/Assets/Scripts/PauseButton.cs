using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PauseButton : MonoBehaviour
{
    /*
        Uzupelnij dwie funkcje ponizej
      
        Time.timeScale = 0 >> zapauzowanie
        Time.timeScale = 1 >> wznowienie
        Input.GetKeyDown(KeyCode.X) >> użytkownik nacisnął X na klawiaturze

    */
    /*
      
     // pauza za pomoca buttonu

    public void Pause()
    {
        if (...==...)
        {
            Time.timeScale = ...;
        }
        else if (...==...)
        {
            Time.timeScale = ...; 
        }
    }
    */
    bool isPaused = false;
    /*
      
    // pauza klawiszem spacji
    
    void Update()
    {
        if (...)
        {

            if (isPaused == ...)
            {

                isPaused = ...;
                Time.timeScale = ...;
            }

            else if (isPaused == ...)
            {

                isPaused = ...;
                Time.timeScale = ...;
            }

        }
    }
    */
}
