using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Racket2AI : MonoBehaviour
{
    /*
        
        GetComponent<Rigidbody2D>().velocity = new Vector2(wspolrz1,wspolrz2) * predkosc;
        >> nadawanie kierunku i rozpoczecie ruchu

    */
    /*
    public float movementSpeed;

    public GameObject ball;

    private void FixedUpdate()
    {
        if (Mathf.Abs(this.transform.position.y - ball.transform.position.y) > 10)      // mniejsza liczba >> AI szybciej zaczyna reagowac
        {
            if (transform.position.y < ball.transform.position.y)
                // ...
            else
                // ...
        }
        else
            // ...

    }
    */
}
